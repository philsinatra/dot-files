# If you come from bash you might have to change your $PATH.
# export PATH=$HOME/bin:$HOME/.local/bin:/usr/local/bin:$PATH

# Path to your Oh My Zsh installation.
export ZSH="$HOME/.oh-my-zsh"

# Set name of the theme to load --- if set to "random", it will
# load a random theme each time Oh My Zsh is loaded, in which case,
# to know which specific one was loaded, run: echo $RANDOM_THEME
# See https://github.com/ohmyzsh/ohmyzsh/wiki/Themes
# ZSH_THEME="robbyrussell"
ZSH_THEME="wedisagree"

# Set list of themes to pick from when loading at random
# Setting this variable when ZSH_THEME=random will cause zsh to load
# a theme from this variable instead of looking in $ZSH/themes/
# If set to an empty array, this variable will have no effect.
# ZSH_THEME_RANDOM_CANDIDATES=( "robbyrussell" "agnoster" )

# Uncomment the following line to use case-sensitive completion.
# CASE_SENSITIVE="true"

# Uncomment the following line to use hyphen-insensitive completion.
# Case-sensitive completion must be off. _ and - will be interchangeable.
# HYPHEN_INSENSITIVE="true"

# Uncomment one of the following lines to change the auto-update behavior
# zstyle ':omz:update' mode disabled  # disable automatic updates
# zstyle ':omz:update' mode auto      # update automatically without asking
# zstyle ':omz:update' mode reminder  # just remind me to update when it's time

# Uncomment the following line to change how often to auto-update (in days).
# zstyle ':omz:update' frequency 13

# Uncomment the following line if pasting URLs and other text is messed up.
# DISABLE_MAGIC_FUNCTIONS="true"

# Uncomment the following line to disable colors in ls.
# DISABLE_LS_COLORS="true"

# Uncomment the following line to disable auto-setting terminal title.
# DISABLE_AUTO_TITLE="true"

# Uncomment the following line to enable command auto-correction.
# ENABLE_CORRECTION="true"

# Uncomment the following line to display red dots whilst waiting for completion.
# You can also set it to another string to have that shown instead of the default red dots.
# e.g. COMPLETION_WAITING_DOTS="%F{yellow}waiting...%f"
# Caution: this setting can cause issues with multiline prompts in zsh < 5.7.1 (see #5765)
# COMPLETION_WAITING_DOTS="true"

# Uncomment the following line if you want to disable marking untracked files
# under VCS as dirty. This makes repository status check for large repositories
# much, much faster.
# DISABLE_UNTRACKED_FILES_DIRTY="true"

# Uncomment the following line if you want to change the command execution time
# stamp shown in the history command output.
# You can set one of the optional three formats:
# "mm/dd/yyyy"|"dd.mm.yyyy"|"yyyy-mm-dd"
# or set a custom format using the strftime function format specifications,
# see 'man strftime' for details.
# HIST_STAMPS="mm/dd/yyyy"

# Would you like to use another custom folder than $ZSH/custom?
# ZSH_CUSTOM=/path/to/new-custom-folder

# Which plugins would you like to load?
# Standard plugins can be found in $ZSH/plugins/
# Custom plugins may be added to $ZSH_CUSTOM/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
# Add wisely, as too many plugins slow down shell startup.
plugins=(
    git
    zsh-autosuggestions
)

source $ZSH/oh-my-zsh.sh

# User configuration

# export MANPATH="/usr/local/man:$MANPATH"

# You may need to manually set your language environment
# export LANG=en_US.UTF-8

# Preferred editor for local and remote sessions
# if [[ -n $SSH_CONNECTION ]]; then
#   export EDITOR='vim'
# else
#   export EDITOR='nvim'
# fi

# Compilation flags
# export ARCHFLAGS="-arch $(uname -m)"

# Set personal aliases, overriding those provided by Oh My Zsh libs,
# plugins, and themes. Aliases can be placed here, though Oh My Zsh
# users are encouraged to define aliases within a top-level file in
# the $ZSH_CUSTOM folder, with .zsh extension. Examples:
# - $ZSH_CUSTOM/aliases.zsh
# - $ZSH_CUSTOM/macos.zsh
# For a full list of active aliases, run `alias`.
#
# Example aliases
# alias zshconfig="mate ~/.zshrc"
# alias ohmyzsh="mate ~/.oh-my-zsh"
alias dt="cd ~/Desktop"
alias dl="cd ~/Downloads"
alias dc="cd ~/Documents"
# alias nv="nvim "
alias mamp="/Applications/MAMP/Library/bin/mysql80/bin/mysql --host=localhost --port=8889 -u root -proot"

# eza (Modern ls replacement)
alias ls='eza'
alias ll='eza -l --icons'          # Long format with icons
alias la='eza -la --icons'         # Long format, hidden files
alias lt='eza --tree --level=2'    # Directory tree
alias lzg='lazygit'
alias lzs='lazysql'
alias md='ekphos'

alias c='opencode'
alias cx='printf "\033[2J\033[3J\033[H" && claude'
# alias cx='printf "\033[2J\033[3J\033[H" && claude --allow-dangerously-skip-permissions'
alias t="tmux new"
# alias t='tmux attach || tmux new -s Work'
n() { if [ "$#" -eq 0 ]; then command nvim . ; else command nvim "$@"; fi; }

alias claude-mem='bun "/Users/philipsinatra/.claude/plugins/marketplaces/thedotmack/plugin/scripts/worker-service.cjs"'

# fd (Modern find)
alias find='fd'
alias f='fd'   # shorthand

# fzf
if [[ -f ~/.fzf.zsh ]]; then
  source ~/.fzf.zsh
fi

ff() {
    local file
    file=$(fd --type f --hidden --follow --exclude .git | fzf) && echo "Selected: $file" && ${EDITOR:-nvim} "$file"
}

# Optional: fzf keybindings and completions
# [ -f /usr/share/fzf/key-bindings.zsh ] && source /usr/share/fzf/key-bindings.zsh
# [ -f /usr/share/fzf/completion.zsh ] && source /usr/share/fzf/completion.zsh

# Use fd as fzf's backend (much faster)
export FZF_DEFAULT_COMMAND='fd --type f --hidden --follow --exclude .git'
export FZF_CTRL_T_COMMAND="$FZF_DEFAULT_COMMAND"
export FZF_ALT_C_COMMAND='fd --type d --hidden --follow --exclude .git'
export FZF_DEFAULT_OPTS='--height 40% --layout=reverse --border'

# ripgrep (rg as default grep)
alias grep='rg --smart-case --hidden --glob "!.git/*"'

#  Custom
function mkd() {
    mkdir -p "$@" && cd "$_";
}

function touchp() {
    mkdir -p "$(dirname "$1")" && touch "$1";
}

function y() {
	local tmp="$(mktemp -t "yazi-cwd.XXXXXX")" cwd
	command yazi "$@" --cwd-file="$tmp"
	IFS= read -r -d '' cwd < "$tmp"
	[ "$cwd" != "$PWD" ] && [ -d "$cwd" ] && builtin cd -- "$cwd"
	rm -f -- "$tmp"
}

function tmux-3up-1down() {
  tmux new-window \; \
    split-window -v -p 20 \; \
    select-pane -t 0 \; \
    split-window -h \; \
    split-window -h \; \
    select-pane -t 0 \; \
    resize-pane -x "25%" \; \
    select-pane -t 2 \; \
    resize-pane -x "25%"
}

alias t3='tmux-3up-1down'

# PATH configuration (consolidated)
export PATH="$HOME/.local/bin:$HOME/.opencode/bin:/opt/homebrew/bin:/opt/homebrew/opt/postgresql@14/bin:$HOME/.codeium/windsurf/bin:$PATH"

# MAMP (uncomment if needed)
# export PATH="/Applications/MAMP/bin/php/php9.3.0/bin:$PATH"
# export PATH="/Applications/MAMP/bin/php/:$PATH"

# NVM (lazy loaded for faster shell startup)
export NVM_DIR="$HOME/.nvm"
nvm() {
  unset -f nvm node npm npx
  [ -s "/opt/homebrew/opt/nvm/nvm.sh" ] && \. "/opt/homebrew/opt/nvm/nvm.sh"
  [ -s "/opt/homebrew/opt/nvm/etc/bash_completion.d/nvm" ] && \. "/opt/homebrew/opt/nvm/etc/bash_completion.d/nvm"
  nvm "$@"
}
node() { nvm use default >/dev/null 2>&1; unset -f node; node "$@"; }
npm() { nvm use default >/dev/null 2>&1; unset -f npm; npm "$@"; }
npx() { nvm use default >/dev/null 2>&1; unset -f npx; npx "$@"; }

# Initialize Zoxide for Yazi file manager
export _ZO_DATA_DIR="$HOME/.local/share/zoxide"
eval "$(zoxide init zsh)"

export EDITOR=nvim

# mise (runtime version manager)
eval "$(~/.local/bin/mise activate zsh)"

# Tmux Developer Layout (tdl) - Omarchy-style port for macOS / Zsh
# NEW LAYOUT: AI agents stacked on the LEFT | Neovim (full height) on the RIGHT
# Usage:
#   tdl c          → OpenCode (left, full height) + big Neovim (right)
#   tdl c cx       → Left column: OpenCode (top) + safe Claude (cx, bottom) | big Neovim (right)
#   tdl codex      → Same layout with Codex (or any agent)
# Customize EDITOR_WIDTH_PERCENT below if you want more/less space for Neovim.
tdl() {
  # ================== CONFIG ==================
  local EDITOR_WIDTH_PERCENT=65     # ← Neovim width (bigger = 70–75, smaller agents = 60)
  # ===========================================

  # Create project-specific session name
  local project_name
  project_name=$(basename "$PWD")
  project_name=${project_name//[^a-zA-Z0-9_-]/_}
  local session_name="dev-${project_name}"

  local agent1="${1:-c}"
  local agent2="$2"

  # If outside tmux → create/attach the project session
  if [[ -z "$TMUX" ]]; then
    if ! tmux has-session -t "$session_name" 2>/dev/null; then
      tmux new-session -d -s "$session_name" -c "$PWD" -n "dev"
    fi
    tmux attach-session -t "$session_name"
    return 0
  fi

  # === Inside tmux: build the exact layout ===
  tmux new-window -n "dev" -c "$PWD"

  # 1. Split horizontally: Left (AI column) | Right (Neovim full height)
  tmux split-window -h -c "$PWD" -p "$EDITOR_WIDTH_PERCENT" -t 0

  if [[ -n "$agent2" ]]; then
    # 2. Split LEFT column vertically
    tmux split-window -v -c "$PWD" -p 50 -t 0

    # Pane layout is now guaranteed:
    #   0 = Top-left     → OpenCode (c)
    #   1 = Bottom-left  → Claude (cx)
    #   2 = Right        → Neovim (n) full height

    # Tiny delays prevent race conditions (agent CLIs take ~300ms to start)
    sleep 0.25
    tmux send-keys -t 0 "$agent1" Enter
    sleep 0.35
    tmux send-keys -t 1 "$agent2" Enter   # ← now correctly targets bottom-left
    sleep 0.25
    tmux send-keys -t 2 "n" Enter         # ← now correctly targets right pane

    tmux select-pane -t 2                 # Focus Neovim on the right
  else
    # Single-agent fallback (left = agent | right = Neovim)
    sleep 0.25
    tmux send-keys -t 0 "$agent1" Enter
    sleep 0.25
    tmux send-keys -t 1 "n" Enter
    tmux select-pane -t 1
  fi
}
